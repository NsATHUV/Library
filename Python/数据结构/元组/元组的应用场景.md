---
tags:
  - Python/内容
aliases: 
日月: 2023-12-31
时分: 23:12
---
### 元组的应用场景

**问题：** 如果想要存储多个数据，但是这些数据是不可修改的，怎么办？

**答案：** 列表？列表可以一次性存储多个数据，但是列表中的数据允许修改。

```python
num_list = [10, 20, 30]
num_list[0] = 100
```

**一种解决方案：** 使用元组。元组可以存储多个数据，而且元组内的数据是不可修改的。